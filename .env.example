# =============================================================================
# Jobper v5.0 — Variables de entorno
# Copia a .env y llena los valores. NUNCA subas .env a git.
# =============================================================================

# --- Base de datos ---
# SQLite (dev):      sqlite:///jobper.db
# PostgreSQL (prod): postgresql://user:pass@host:5432/jobper
# Railway provee DATABASE_URL automaticamente.
DATABASE_URL=sqlite:///jobper.db

# --- Auth (REQUERIDO) ---
# Genera con: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=
FRONTEND_URL=http://localhost:3000

# --- Email (REQUERIDO para magic links y alertas) ---
# https://resend.com → API Keys
RESEND_API_KEY=
RESEND_FROM_EMAIL=Jobper <noreply@jobper.co>

# --- Pagos manuales (Nequi / Bancolombia) ---
NEQUI_NUMBER=
BANCOLOMBIA_ACCOUNT=
BANCOLOMBIA_TYPE=Ahorros
BANCOLOMBIA_HOLDER=

# --- Admin ---
ADMIN_TOKEN=cambiar-en-produccion
ADMIN_EMAIL=tu@email.com

# --- Redis (opcional — cache + Celery) ---
REDIS_URL=

# --- Elasticsearch (opcional — mejora busquedas) ---
ELASTICSEARCH_URL=

# --- Web Push (opcional) ---
# Genera con: npx web-push generate-vapid-keys
VAPID_PRIVATE_KEY=
VAPID_PUBLIC_KEY=
VAPID_CLAIMS_EMAIL=mailto:soporte@jobper.co

# --- WhatsApp Cloud API (opcional) ---
# https://developers.facebook.com → WhatsApp Business
WHATSAPP_API_TOKEN=
WHATSAPP_PHONE_ID=

# --- Flask ---
FLASK_DEBUG=false
PORT=5001
CORS_ORIGINS=http://localhost:3000
