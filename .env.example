# =============================================================================
# JOBPER BOT v3.0 (Premium) - Variables de Entorno
# =============================================================================
# Copia este archivo a .env y completa con tus credenciales reales.
# NUNCA subas el archivo .env a un repositorio publico.
# =============================================================================

# -----------------------------------------------------------------------------
# BASE DE DATOS
# -----------------------------------------------------------------------------
# PostgreSQL para produccion (Railway/Render proveen esta variable automaticamente)
# Formato: postgresql://usuario:password@host:puerto/nombre_db
# Si no se configura, usa SQLite local (jobper.db)
DATABASE_URL=postgresql://user:password@host:5432/jobper

# -----------------------------------------------------------------------------
# TWILIO - Configuracion de WhatsApp
# -----------------------------------------------------------------------------
# Obten estas credenciales en: https://console.twilio.com/
#
# TWILIO_SID: Tu Account SID (empieza con "AC")
TWILIO_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# TWILIO_TOKEN: Tu Auth Token
TWILIO_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# TWILIO_FROM: Numero de WhatsApp de Twilio (sin el prefijo "whatsapp:")
# Para sandbox usa: +14155238886
# Para produccion: tu numero aprobado por Twilio
TWILIO_FROM=+14155238886

# -----------------------------------------------------------------------------
# APIs DE LICITACIONES
# -----------------------------------------------------------------------------

# SAM.GOV - API de Licitaciones USA
# Obten tu API Key en: https://sam.gov/content/entity-information
# 1. Crear cuenta en sam.gov
# 2. Solicitar System Account con acceso a Public API
# 3. Esperar aprobacion (1-3 dias)
SAM_API_KEY=tu_api_key_de_sam_gov

# -----------------------------------------------------------------------------
# NLP / EMBEDDINGS
# -----------------------------------------------------------------------------
# Modelo de embeddings (opcional, usa el default si no se especifica)
# Default: paraphrase-multilingual-MiniLM-L12-v2
# EMBEDDING_MODEL=paraphrase-multilingual-MiniLM-L12-v2

# -----------------------------------------------------------------------------
# SERVIDOR FLASK
# -----------------------------------------------------------------------------
# Puerto del servidor (Railway/Render asignan PORT automaticamente)
PORT=5000

# Modo debug (solo para desarrollo)
FLASK_DEBUG=false

# -----------------------------------------------------------------------------
# ADMINISTRACION
# -----------------------------------------------------------------------------
# Token para endpoints de admin (CAMBIAR en produccion!)
ADMIN_TOKEN=cambiar-este-token-en-produccion
