# =============================================================================
# JOBPER v5.0 — CRM de Contratos para Colombia
# =============================================================================
# Versiones pineadas con ~= (compatible release):
#   ~=X.Y  =>  >=X.Y, ==X.*  (permite parches, bloquea saltos de versión mayor)
#
# Para actualizar: pip-compile requirements.in  o  pip freeze > requirements.lock
# Para reproducir exacto en producción: usar requirements.lock (si existe)
# =============================================================================

# Web Framework
flask~=3.0.0
flask-cors~=4.0.0
gunicorn~=22.0.0

# Base de datos
sqlalchemy~=2.0.0
psycopg2-binary~=2.9.0
alembic~=1.13.0

# Auth
PyJWT~=2.9.0
bcrypt~=4.2.0

# Validation
pydantic[email]~=2.9.0

# Cache + Queue
redis~=5.2.0
celery~=5.4.0

# Search
elasticsearch~=8.15.0

# HTTP Client
requests~=2.32.0

# Push notifications
pywebpush~=2.0.0

# Scheduler
schedule~=1.2.0

# Variables de entorno
python-dotenv~=1.0.0

# =============================================================================
# AI / LLM
# =============================================================================
openai~=1.50.0

# =============================================================================
# NLP / EMBEDDINGS
# =============================================================================
sentence-transformers~=3.2.0
numpy~=1.26.0
# torch instalado separadamente como CPU-only en Dockerfile (ahorra 5GB):
# pip install torch --index-url https://download.pytorch.org/whl/cpu

# =============================================================================
# SCRAPERS
# =============================================================================
beautifulsoup4~=4.12.0
lxml~=5.3.0

# =============================================================================
# UTILIDADES
# =============================================================================
python-dateutil~=2.9.0
pytz~=2024.1
bleach~=6.1.0
openpyxl~=3.1.0
